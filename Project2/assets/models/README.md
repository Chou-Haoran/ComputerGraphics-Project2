# assets/models

所有场景模型都放在 `assets/models/` 下，每个场景单独一个子目录。

## 目录约定

- 一个场景族放一个目录。
- `.scene` 文件与它引用的 OBJ 资源放在一起。
- 贴图路径尽量写成相对场景目录的路径。
- 导出 OBJ 时保留法线和 UV，并在导出前先三角化。

## 材质挂接方式

- `material = ...`：整个 mesh 统一一种材质类型
- `materialMap = "..."`：按 MTL 子材质名分别覆盖
